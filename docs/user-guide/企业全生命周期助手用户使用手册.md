# 企业全生命周期助手用户使用手册

适用版本：V1.3.1

本手册面向共创团队成员。企业全生命周期助手由56个 Skills、团队云端知识库、REST API和MCP组成，可导入支持 Skills 的 Agent 使用。

## 1. 注册方式

### 注册前提

管理员须先将你的中文真实姓名和企业微信绑定手机号后四位加入注册邀请名单。批量导入的名单同样有效，不必等待管理员单独发送邀请链接。

### 自助注册

1. 打开团队网站，进入“注册账号”。
2. 填写英文登录账号。账号长度为3至32位，可包含数字、点、下划线和连字符。
3. 填写中文真实姓名。
4. 填写企业微信绑定手机号后四位。同名成员使用手机号后四位区分。
5. 输入公司全称 `共创集团` 完成团队验证。
6. 设置不少于9位的登录密码并提交。

每位成员只能注册一个账号。姓名或手机号后四位未命中邀请名单时，页面会提示联系管理员；管理员删除或停用成员后，该账号不能继续访问，重新加入名单后方可恢复注册资格。

### 登录

- 使用英文账号和密码登录。
- 私人设备可勾选“七天自动登录”；公共电脑不建议勾选。
- 登录后显示中文真实姓名，右上角菜单可进入账号安全或退出登录。

### 找回密码

1. 在登录页点击“验证身份并重置”。
2. 填写英文登录用户名、中文真实姓名和所属公司全称。
3. 三项信息与注册资料完全一致后，设置不少于9位的新密码。
4. 重置成功后，原有网站登录会话立即失效；个人 API 访问凭据不受影响。

### 留言反馈

登录后进入“留言反馈”，可提交故障与 Bug、知识内容纠错、功能建议或其他意见。成员可以查看自己的处理进度和管理员回复；管理员可以查看全部留言并更新为待处理、处理中、已解决或已关闭。

## 2. 下载与安装及使用方法

### 网站直接使用

登录后可在智能驾驶舱直接提问。系统优先检索团队知识库，未命中时才按宿主能力联网核验。普通成员使用团队大模型时遵守网站显示的每日额度；管理员不限次数。用户填写自己的兼容模型 API 后，不占团队问答额度。

### 安装 Skills

1. 登录网站，进入“Skills 下载”。
2. 根据宿主选择 V1.3.1 通用 Skills ZIP 或 V1.3.1 WorkBuddy 插件 ZIP。通用包按宿主要求导入；WorkBuddy 包使用随包提供的一键安装器，并在透明安装计划中确认来源、哈希、本机改动和撤销命令。两种版本业务能力相同，不要重复安装。V1.0至V1.2可在历史版本区下载。
3. 将 `skills` 文件夹拖入或导入当前 Agent 的 Skills 目录或工作区。
4. 在 Agent 中输入：

```text
请检查企业全生命周期助手是否安装完整，并启动首次配置向导。
```

若Agent不能直接识别 `SKILL.md`，使用Agent提供的Skill导入或转换能力。

首次安装后再输入：

```text
帮我安装OCR、PDF、Word、PPT、Excel和联网检索这几个Skills
```

通用文档、OCR和联网能力由宿主 Agent 提供，不在企业全生命周期助手中重复打包。

### 连接团队知识库

1. 登录网站，进入“连接我的 Agent”。
2. 点击“复制给 Agent”。
3. 将复制的整段文字粘贴到当前本地 Agent 对话框。
4. Agent 自动识别 macOS 或 Windows 和当前宿主，完成设备登记、MCP 配置与连接验证。

普通成员无需查看或保存个人 API Key、设备私钥和 MCP 请求头。同一账号同时只能绑定一台设备；换机时先在门户点击“更换绑定设备”，再把新的配置发送给新设备 Agent。管理员账号不执行该设备限制。

### 个人偏好与跨设备同步

右上角用户菜单进入“我的使用习惯”。日常只需修改页面选项并保存：

- 同一账号换电脑后自动继承地区、格式、详细程度、语气和术语习惯。
- 改错时可撤销上一版或恢复官方默认，历史记录不会删除。
- 对Agent说“默认这么写”或“记住我的习惯”时，Agent自动保存为个人习惯，不再修改正式Skill。
- 旧版Skill曾被手工修改时，对Agent说“请迁移我的旧版Skills个人习惯，并同步到云端”。系统自动备份旧文件、接收安全习惯并列出无法自动处理的内容。

本地文件、偏好API和三方合并属于后台实现，普通用户不需要手工操作。

### 知识库连接完整验收

可让 Agent 按以下顺序执行，不必把凭据发给管理员：

1. `GET /v1/me`：返回当前英文登录账号。
2. `POST /v1/search`：检索一个非敏感公开关键词。
3. `GET /v1/documents/{id}`：读取上一步命中的文档。
4. `GET /v1/usage`：返回当前用户调用记录。
5. MCP初始化并读取工具列表，再调用知识服务状态或知识检索工具。

五步全部成功才算REST与MCP完整端到端验收通过。OAuth 客户端还应确认能够自动打开登录授权页并在授权后加载工具列表。仅打开网站或仅返回401不算通过。

### 可选外部能力

- 天眼查：用于企业主体、工商变更、历年名称和当前登记地区的首轮分诊。
- 企查查：增强工商、股权、风险和知识产权画像，可通过网站保留的企查查入口注册并按供应商说明配置API或MCP。
- BigQuery Patents：增强全球专利批量检索、同族和引证分析。
- 浏览器或Playwright：用于政府官网核验和合法登录后的网页任务。
- 企策顾问：仅作项目发现线索，正式条件、金额、日期和名单必须回到政府原文核验。

所有外部能力由统一首次配置向导检测，避免每个 Skill 重复配置。

## 3. 能力边界

- 覆盖政府项目检索、匹配、体检、材料编写、企业分析、专利、法律法规、制造业财税风险和交付归档，但不承诺一定符合或一定获批。
- 系统后台自动执行政策来源排序与版本核验；用户只需核对结论引用的文件名称、来源链接、效力状态和当期通知。
- 云端知识库由管理员统一上传、修改、删除和重建索引；成员的地区政策增改删保存在本人本地工作区，不写入云端。
- 无可靠数据时不推算企业财务，不以第三方过期数据替代客户材料。
- 审中专利不能作为授权成果使用；专利任务必须区分有效授权、审中、失效和转让取得。
- 专利检索、FTO和侵权预警属于初筛，不替代专利代理师或律师的正式意见。
- 法律 Skill 可做法规检索、效力核验、公司法与项目合规自查，不替代执业律师针对具体交易、争议或诉讼出具的法律意见。
- OCR结果不是天然准确，企业名称、名单序号、金额、日期和表格列必须抽样复核。
- 当前检索层未命中只能说明“未命中”，不能据此断言资料或项目不存在。

## 4. 技能覆盖

### 总入口、配置与知识能力

`project-application-assistant`、`project-task-router`、`first-run-configuration`、`local-knowledge-retrieval`、`project-memory`、`evidence-ledger`、`graphify`、`project-deliverable-archive`。

### 政策检索、项目匹配与规则维护

`policy-retrieval`、`project-matching`、`project-feasibility`、`project-rule-manager`、`third-party-data-indexing`、`web-task-operator`、`peer-benchmarking`、`industry-positioning`、`industry-chain-foundation-matcher`。

### 项目类别

`sme-score-preassessment`、`sme-development-projects`、`high-tech-enterprise-preassessment`、`technology-innovation-projects`、`industrialization-projects`、`digitalization-projects`、`green-development-projects`、`quality-brand-projects`、`intellectual-property-projects`、`talent-projects`、`investment-subsidy-projects`、`trade-and-open-economy-projects`、`agriculture-and-rural-projects`、`regional-special-projects`。

### 企业分析、申报材料与财税法律

`enterprise-profile`、`enterprise-panorama-analysis`、`application-writing`、`application-version-diff`、`consistency-check`、`financial-verification`、`manufacturing-tax-risk-analysis`、`jiaotang-legal-regulations`。

### 标准起草

`standard-drafting`：按现行GB/T 1.1及适用专项规则完成标准立项分析、结构设计、条款起草、编制说明、征求意见处理和一致性审查。

### 专利与知识产权

`jiaotang-patent-router`、`checking-patdocx-cn-single-agent`、`ip-assessment`。公司级专利工作统一进入总路由；中国专利申请 Word 核稿保持独立。

### 受控自进化

`skill-authoring`、`skill-curator`、`skill-evolution`、`evolution-governance`、`experience-recorder`。

安装后受控自进化、纠错记录、影响分析和任务结束四问复盘默认启用，但正式团队 Skill 的修改和发布仍需人工审批。

## 5. 法律 Skill 能做什么

`jiaotang-legal-regulations` 负责把企业事实与现行法律法规建立证据链，主要输出：

- 按国家、省、市、区县和行业生成法规适用矩阵。
- 识别政策或规范文件的现行、历史、废止、失效、替代和过渡状态。
- 检查项目承诺、重复申报、财政资金、信用惩戒、材料真实性和知识产权状态风险。
- 按新公司法检查注册资本与出资、章程、治理、董监高责任、关联交易、股权转让、利润分配、减资、清算和注销。
- 对研发财税、知识产权、客户资料和数据处理给出合规红线、补证清单和整改计划。

标准输出顺序为“事实、适用规则、判断、风险、补证材料、整改动作”。涉及具体合同、交易、争议、行政处罚或诉讼时，应转交执业律师复核。

## 6. 常用指令

### 安装与连接

```text
请检查企业全生命周期助手是否安装完整，并启动首次配置向导。
```

```text
请测试团队知识库REST API和MCP是否完整可用，并逐步报告 /v1/me、检索、文档读取、用量和MCP工具调用结果。
```

### 企业与项目

```text
帮我出具这家企业全景分析报告，先列事实来源，再给风险和合作建议。
```

```text
分析这家企业目前能申报哪些政府项目，按国家、省、市、区县分层，并给出未来五年规划。
```

```text
这家企业想申报研究院，请核验当前项目名称、现行政策、申报条件、差距和材料清单。
```

```text
查询这家企业同行中有哪些专精特新或小巨人企业，列出名单序号、批次、地区和原始来源。
```

```text
请对这家企业做高企申报前评估，比较今年与明年的硬门槛、成长性、三档评分和补强计划。
```

```text
请先生成专精特新或小巨人申报前评分用的三年财务底表，待我回传并确认项目版本后再评分。
```

### 专精特新和小巨人

```text
请先识别并让我确认申请书项目类型和版本，再体检这份材料。
```

```text
按现行优质中小企业梯度培育管理办法，分析企业差距，并对主导产品、补短板、填空白、国产替代分别给出保留、替换或补证后保留结论。
```

```text
严格按照包内产业链架构和工业六基目录匹配主导产品；没有直接分类时，给一个相似分类和一个分析判断分类。
```

### 专利

```text
根据现有资料和项目要求，为这家企业规划10个专利方向，并说明每个方向与产品、技术问题和项目指标的关系。
```

```text
对这项产品做FTO初筛，区分公开文本、授权文本和法律状态，并列出仍需人工核验的权利要求。
```

```text
审查这份专利交底书或申请文件，检查权利要求依附关系、保护范围、支持性和提交前风险。
```

### 法律与财税

```text
帮我排查这么做的法律风险，按事实、适用法规、风险等级、补证材料和整改动作输出。
```

```text
按新公司法为这家企业做合规自查，重点检查出资、章程、治理、关联交易、股权转让、减资和清算风险。
```

```text
帮我出具这家企业金税四期分析报告，复算近三年财务指标，区分已证实风险和待补证信号。
```

### 政策版本与材料审查

```text
核验这个政策是否仍然有效；如已废止，给出废止或替代依据、新项目名称、现行条件和官方原文。
```

```text
帮我体检这份申报材料，检查数字、事实、主导产品、知识产权和章节之间的一致性。
```

```text
对比这两个版本的申报材料，列出数据变化、表述升级、遗漏和新增风险。
```

### 标准撰写

```text
请按现行GB/T 1.1帮我起草这项企业标准。先判断标准类型，核验法规和现行同类标准，再输出标准设计、要求—试验方法—判定规则矩阵、标准草案和编制说明。
```

```text
请审查这份标准草案，重点检查范围、规范性引用文件、术语、助动词、技术指标来源、要求与试验方法对应、检验规则和专利风险。
```

## 7. 更新与问题处理

- 网站“Skills 下载”显示最新版、发布时间和更新说明。
- 升级时运行 `first-run-configuration/scripts/upgrade_inheritance.py`。新版官方核心、个人覆盖层和云端偏好分别继承，自动生成升级继承报告。
- 升级工具以旧官方、当前安装和新官方三份 `SKILL.md` 哈希识别用户直接修改；旧文件进入可恢复备份，不会覆盖新版核心。需要保留的个人习惯应转为结构化偏好。
- 出现401：检查个人凭据是否完整、是否已被吊销，再重新生成并执行五步验收。
- 知识库未命中：确认地区、年度、项目别名和政策版本，再请求官方联网核验。
- 文档无法解析：先使用宿主OCR或文档能力转换为可检索PDF或Markdown，再由管理员上传。

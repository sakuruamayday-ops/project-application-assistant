# 共创研究院企业全生命周期助手 V1.6.18 发布说明

V1.6.18 以 V1.6.17 正式版为上一基线，修复制造企业财税报告中生成器、确定性计算与专业交付契约互相不一致的问题。该版本已于 2026-09-06 完成 GitHub Latest、成员门户、隔离安装、本机同步和客户端技能更新源的正式发布与回读。

## 一、本版本变更

V1.6.18 修复制造企业财税报告的生成器、确定性计算与专业交付契约不一致。

### 套件清单声明的版本事实

- 制造企业财税报告生成器固定输出财务总览、风险地图、90天整改路线和计算过程与来源四张契约表，并与可见中文章节顺序一致。
- 确定性计算器新增营业收入与应收账款同比、研发费用率、资产负债表恒等式差额及无法计算原因，报告必须直接消费该回执。
- 报告生成时核对指标文件的结构和企业主体，避免模型遗漏、手工改写或跨企业误用计算结果。
- 技能发布继续对全部随包模板执行解析、保存回读和真实渲染，并对项目报告母版逐一核对交付契约。
- 全部技能统一声明隐藏工具须经宿主 run_code 调用，并禁止为理解命令预读脚本、模板、示例或测试，减少无效工具试错和上下文膨胀。
- V1.6.18 继承 V1.6.17 的文档读取、专业交付有界收束、模型与推理档位保持及同源技能投影能力。

- 财税报告固定生成“财务总览”“风险地图”“90天整改路线”“计算过程与来源”四张契约表，并按报告可见中文章节校验顺序。
- 确定性计算器自动输出营业收入同比、应收账款同比、研发费用率、资产负债表恒等式差额和无法计算原因。
- 报告生成器强制读取 `manufacturing-tax-risk-metrics/v1`，核对企业主体，并把确定性复算直接写入报告，避免模型遗漏或手工改写。
- 主技能没有专用模板或生成器时，客户端通过已签名 `evidence-ledger.create-docx` 将已预校验正文一次生成原生 DOCX，禁止临时脚本、环境探查和成品回读循环；专用模板仍保持最高优先级。
- 兜底 DOCX 统一采用紧凑 A4 版式；同一份 2796 字、2 个表格的质量品牌测试稿经 WPS 实际渲染由 4 页收敛为 3 页，避免内容校验通过但违反用户页数上限。
- 共享品牌运行时及同类 PDF 脚本统一使用 PyMuPDF 的当前导入入口，避免依赖弃用警告污染受签名操作的单一 JSON 回执。
- 51 项技能统一约束隐藏工具必须通过宿主 `run_code` 调用，先执行已记录命令，只有真实报错且契约不明确时才读取直接相关源码，避免根级工具失败和无效上下文膨胀。
- 发布前对全部 29 个 Office 模板和 4 个源码模板执行解析、保存回读与真实渲染，对 24 个项目报告母版逐一核对交付契约。
- 继承 V1.6.17 的文档读取、专业交付有界收束、模型与推理档位保持及同源技能投影能力。

## 二、发布验收

正式包通过 13 项发布检查、全部随包模板解析与实渲染、51 项逐技能清单与 Ed25519 签名、隔离安装、门户切换、客户端技能更新清单回读和本机同步。通用包 SHA-256 为 `5045219a7378ac2c37f53db94249a28f3fc6b1595b281d5b88f398821343196a`，客户端技能更新包 SHA-256 为 `6228cb38a0cb227798a9bd00cd277041dcb175408b026d3953a63987618fec53`；正式发布事务 SHA-256 为 `a0349a96adce0999281cbd0b6bc2a23e896e5196fc10de1e92942f8b0df540ac`。

- macOS 与 Windows 客户端均消费 V1.6.18 的同一签名技能身份；两端安装包、更新和回滚仍由各自客户端发布事务独立验收。
- 通用包不携带用户凭据、客户资料或本地知识索引，企业数字身份证不进入插件包或技能包。

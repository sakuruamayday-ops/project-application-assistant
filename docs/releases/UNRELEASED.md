# 未发布修改记录

本文件只记录 V1.4.3 发布后的待发布修改，不预先分配下一版本号。

## 记录规则

- 每项修改记录日期、影响范围、主要文件和已完成测试。
- 未收到主人明确的“发布”指令前，不修改`suite-manifest.json`中的正式版本，不生成发布ZIP、SHA-256、签名或发布索引。
- 正式发布时先确认目标版本，并核对套件清单、插件清单、网站展示、下载文件名和发布公告使用同一版本。
- 测试构建只能使用明确标记为临时候选的隔离目录，不得伪装成正式签名发布件。

## 待发布修改

- 2026-08-01｜网站成员管理：按内部使用决定恢复名单成员自助注册。管理员录入中文真实姓名和企业微信手机号后四位后，成员可从普通注册页直接核验并创建账号；专属链接降为可选预填方式。
- 继续保留来源 IP 限流、公司全称校验、数据库 `BEGIN IMMEDIATE` 原子绑定、重复注册阻断、已绑定账号禁止自助重设密码等外围防护。
- V1.4.3 的正式发布记录保持不变；本项属于后续网站热修复，未重新生成或签名技能包。

# 未发布修改记录

本文件只记录V1.3发布后的待发布修改，不预先分配下一版本号。

## 记录规则

- 每项修改记录日期、影响范围、主要文件和已完成测试。
- 未收到主人明确的“发布”指令前，不修改`suite-manifest.json`中的正式版本，不生成发布ZIP、SHA-256、签名或发布索引。
- 正式发布时先确认目标版本，并核对套件清单、插件清单、网站展示、下载文件名和发布公告使用同一版本。
- 测试构建只能使用明确标记为临时候选的隔离目录，不得伪装成正式签名发布件。

## 待发布修改

- 2026年7月27日：发布基础设施改为强制两阶段流程。第一阶段只创建 GitHub
  预发布并在知识门户展示“正式发布中”；第二阶段必须收到主人独立确认语
  “确认正式发布”后，才登记网站正式包并提升为 GitHub Latest。旧的
  `--execute` 一步直发入口已停用。
- 影响范围：`scripts/controlled_release.py`、知识门户发布脚本、数据库迁移、
  Skills 下载页和发布回归测试。

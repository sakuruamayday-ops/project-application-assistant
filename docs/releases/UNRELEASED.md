# 未发布修改记录

本文件只记录V1.3.1.4发布后的待发布修改，不预先分配下一版本号。

## 记录规则

- 每项修改记录日期、影响范围、主要文件和已完成测试。
- 未收到主人明确的“发布”指令前，不修改`suite-manifest.json`中的正式版本，不生成发布ZIP、SHA-256、签名或发布索引。
- 正式发布时先确认目标版本，并核对套件清单、插件清单、网站展示、下载文件名和发布公告使用同一版本。
- 测试构建只能使用明确标记为临时候选的隔离目录，不得伪装成正式签名发布件。

## 待发布修改

- 2026-07-29，修复 WorkBuddy 两步安装协议把需要浏览器登录 Cookie 的门户下载地址交给本地 Agent，导致 Agent 下载到登录页 HTML 并触发 SHA-256 不一致的问题。第二阶段改用已确认安装码作用域内的一次性受限下载地址，并把协议、下载包和引导清单固定到同一已审查 WorkBuddy 发布资产。已在隔离热修复分支完成 192 项门户测试、11 项 WorkBuddy 运行时加固测试、4 项子测试、连接器契约测试、语法检查与差异检查。未修改 V1.3.1.4 ZIP、签名和正式版本号。

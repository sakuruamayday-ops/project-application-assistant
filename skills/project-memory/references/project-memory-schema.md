# 项目记忆结构

项目标识由企业完整名称、项目全称和申报年度组成。

记录分为：`identity` 企业与项目身份、`policy` 已确认政策版本、`facts` 已确认事实及证据编号、`decisions` 用户确认决定、`pending` 待补材料、`artifacts` 交付物路径、`updated_at` 更新时间。

政策、截止日期、企业登记、专利状态和服务状态属于可变事实，超过七天或进入新申报批次时必须重新核验。

个人写作偏好不写入项目事实；密码、密钥、证件原文和无关通信内容禁止保存。

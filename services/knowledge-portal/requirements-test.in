# Direct test dependencies. The generated requirements-test.lock includes the
# complete production graph plus this explicitly pinned test toolchain.
-r requirements.in

pytest==8.4.1

# Word export regression exercises the skill parser.
markdown-it-py==4.2.0

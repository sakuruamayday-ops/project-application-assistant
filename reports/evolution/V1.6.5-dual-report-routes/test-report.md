# V1.6.5 双报告路由测试报告

## 已完成测试

| 测试层 | 结果 |
|---|---|
| 双报告、专精谈单边界、版本清单定向测试 | `19 passed` |
| 门户版本介绍定向测试 | `11 passed` |
| 根目录完整回归 | `290 passed, 2 skipped, 67 subtests passed` |
| 知识门户完整回归 | `593 passed, 7 skipped` |
| JSON 与差异格式 | 通过 |

第一次将门户测试与根目录测试混合收集时，因为门户子项目要求独立 `PYTHONPATH` 出现 `ModuleNotFoundError: app`；按正式子项目入口在 `services/knowledge-portal` 目录设置 `PYTHONPATH=.` 后通过。根目录完整回归第一次仅因发布说明缺少“企业数字身份证不进入插件包”的精确门禁措辞失败，补回发布边界后完整通过。

## 待完成门禁

集合发布器完整门禁、逐技能签名、通用包与两个 WorkBuddy 包构建、临时解压验签、受控发布预发布与正式提升、本机正式同步在提交后执行；结果将追加到正式发布回执。

# 知识索引工具维护候选差异

## 增量发布清单

1. `bind_current_release_manifest` 同时计算候选索引与增量包中的 `release.json`，两份文件不一致立即失败。
2. 本轮清单摘要写入新状态、签名指针和 `prepared-release.json`，覆盖继承的旧摘要。
3. `upload-immutable`、正向 `switch-pointer` 与 `verify-cloud` 均复核包内清单、签名指针及准备记录的一致性。
4. 回滚指针仍按上一状态签名指针处理，不错误地与本轮增量包清单比较。

## 候选库路径保护

1. 删除新增方案中的 `/Users/zsh` 硬编码，改用环境变量和 `Path.home()`。
2. 保留活动索引默认写保护，并允许测试或受控运行显式注入根目录。
3. 对超过阈值且位于配置同步根目录的候选库失败关闭，错误信息给出本地候选根目录。
4. 增加同步目录拒绝、本地候选目录允许和自定义活动索引保护三类测试。

## 明确不执行

- 不提高技能套件版本。
- 不构建、签名或发布插件包。
- 不推送 Git 远端。
- 不上传增量包，不切换 OSS 指针。
- 不修改 `/Users/zsh/JiaotangData/索引/current`。

# 知识索引工具维护测试报告

## 通过项

| 测试层 | 结果 |
|---|---|
| 两个修改模块定向测试 | `30 passed` |
| 知识门户完整回归 | `600 passed, 7 skipped` |
| 根目录正式测试入口 | `290 passed, 2 skipped, 67 subtests passed` |
| Python 编译检查 | 通过 |
| Git 空白与补丁格式检查 | 通过 |

## 过程说明

知识门户完整回归首次有一个新增用例失败，原因是另一项既有测试在同一进程遗留 `JIAOTANG_INDEX_DIR`。新增用例随后显式清除无关环境配置，全套测试通过。根目录直接递归收集会把门户子项目放在错误的模块路径下，产生既有的 `ModuleNotFoundError: app`；按仓库正式分层入口分别执行后，根目录与门户测试均通过。

本机 Python 环境未安装 `ruff`，因此未执行 Ruff；已用 `compileall`、`git diff --check` 和两套完整测试覆盖语法、格式与行为回归。

## 尚未执行

依据用户确认的边界，本批次未执行签名、制品构建、远端推送、OSS 上传、服务器部署、远端指针切换或活动知识索引切换。
